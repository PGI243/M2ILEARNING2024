---
name: Simple CI
on:
  push:
    branches:
      - main
jobs:
  builds:
    runs-on: unbuntu-lastest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: run a one-line script
        run: echo "hello, world!"
      - name: run a one-linescript
        run: echo $"ENVPROD"
      - name: run a one-line script
        run: echo $"PERSOMDP"
      - name: run a one-line script
        run: echo $"LT1"
